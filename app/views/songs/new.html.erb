<h1>New Song</h1><br>
<%= form_for(@song) do |f| %>
  Name: <%= f.text_field :name %><br>

  Genre: <%= f.collection_select :genre_id, Genre.all, :id, :name %><br>

  Artist: <%= f.collection_select :artist_id, Artist.all, :id, :name %>

  <%= f.submit %>
<%end%>